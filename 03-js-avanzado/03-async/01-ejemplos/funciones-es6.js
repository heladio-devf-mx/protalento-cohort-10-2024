function suma(a,b) {
  return a + b;
}

const suma = function (a,b) {
  return a + b;
}

// Arrow function
const suma = (a,b) => {
  return a + b;
}

// Arrow function de una sola línea
const suma = (a,b) => a + b

// Arrow function de un solo parametro
const alCuadrado = a => a*a